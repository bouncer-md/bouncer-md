# Agent Instructions

You are the agent. The policy declares only the escalate outcome.
Per §4.4, escalate has no normative precedence in v0.5 — the resolver
must fall through to block (universal fallback floor).
