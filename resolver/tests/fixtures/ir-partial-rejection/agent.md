# Agent Instructions

You are the agent. This fixture has one valid policy file and one malformed
policy file in the same scope. The resolver must accept the good file, reject
the bad one, and log the rejection — it must not throw or produce an empty IR.
